// Shared constants used across the calendar app.

export const HOUR_MS = 3600000
export const DAY_MS = 86400000

// Fallback colour for calendars that have no calendar-color set. Matches the
// app's accent (also used as a CSS literal in the views' scoped styles).
export const DEFAULT_CALENDAR_COLOR = '#0082c9'
