# web-app-calculator

This application adds a calculator to the search bar of OpenCloud Web. Typing a search term that starts with `=` (e.g. `=1+2`) evaluates the expression via [mathjs](https://mathjs.org/) and shows the result in the search preview.
